apiVersion: 1

datasources:
  - name: vault-rag-metrics
    type: prometheus
    uid: vault-rag-metrics
    access: proxy
    url: http://vault-rag-vmsingle:8428
    editable: false
    jsonData:
      timeInterval: 30s
      httpMethod: POST
