FROM node:22-alpine

RUN apk add --no-cache git openssh-client tini ca-certificates wget \
 && rm -rf /var/cache/apk/*

ENV HOME=/root
WORKDIR /scripts

ENTRYPOINT ["/sbin/tini", "--"]
CMD ["sleep", "infinity"]
